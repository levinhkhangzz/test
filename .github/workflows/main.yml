Skip to content
u400822
/
u400822

Type / to search

Code
Issues
Pull requests
Actions
Projects
Security
Insights
HuyPro
HuyPro #9
Jobs
Run details
Workflow file for this run
.github/workflows/main.yml at 7e3e228
name: HuyPro
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up
        run: |
          git config --global user.name "levinhkhangzz"
          git config --global user.email "levinhkhang631@gmail.com"
      - name: Huy
        run: |
          while true; do
            for ((i=0; i<=1; i++)); do
              echo $i > Huy.txt
              git add .
              git commit -m "By HuyPro"
              git push
            done
          done
